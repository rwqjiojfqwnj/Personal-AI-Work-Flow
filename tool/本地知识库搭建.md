https://my.feishu.cn/docx/YZKkdTxDGob5t2xSxJ1cBjqBnqf

# 本地部署Deepseek

## 下载安装 Ollama

- 官网：https://ollama\.com/download

- 下载 Ollama 后，解压缩然后安装程序

## 下载模型（以 deepseek\-r1:14b 为例，建议根据自己点电脑配置选择）

1. Windows 用按下Win \+R快捷键，输入CMD打开命令提示符，Mac 用户打开终端。

2. 在命令窗口中输入代码 “ollama pull deepseek\-r1:14b” 然后回车。

3. 这个时候就会自动下载这个模型，会看到类似这样的进度，等进度条走到 100%，下载时间取决于你的网速。

```Plain Text
pulling manifest
pulling aabd4debf0c8... 100% ▕████████████████▏ 9.8GB
pulling 369ca498f347... 100% ▕████████████████▏ 387 B
verifying sha256 digest
writing manifest
success
```

4. 进图条走完后，可以直接与这个大模型对话。

5. 下载bge\-m3同理，命令窗口输入“ollama pull bge\-m3”，然后回车

6. 输入“/bye”退出模型。

7. 安装嵌入模型：在命令窗口输入命令 “ollama pull bge\-m3” 回车，然后等待安装完成

8. 下载完成后，可以输入“ollama list”再次检查已安装的模型：

9. 你应该会看到：

```Plain Text
NAME            ID              SIZE    MODIFIED
deepseek-r1:14b  xxxxxxxxxxxx    9.8 GB  Just now
bge-m3          xxxxxxxxxxxx    1.5 GB  1 day ago
```

- 确认 deepseek\-r1:14b、bge\-m3 已经出现在列表中。

10. 再输入 “/bye” 命令，退出安装过程，关闭命令提示符
11. 验证
```
cmd

# 查看模型列表
ollama list

# 查看当前运行的模型
ollama ps

# 先关闭 Copilot 中的 "thinking" 功能（在设置里找到并关闭）
# 然后在命令行中手动测试模型是否能正常响应
ollama run qwen2.5-coder:7b "你好，请简单介绍一下自己"

# 测试 bge-m3 是否能正常加载
ollama run bge-m3 "测试"

```
## 配置环境变量/Windows
===单次有效===
再次打开命令提示符窗口，使用命令 `OLLAMA_ORIGINS=app://obsidian.md* 

或者手动：
===长期有效===
1. 在电脑设置中搜索并打开编辑系统环境变量。
2. 点击右下角环境变量\-系统变量\-新建，变量名填 OLLAMA\_ORIGINS，变量值填 app://obsidian\. md\* ，填完之后点确定保存即可
**重新启动 Ollama（记得重启后再去配置Copilot）**
# Obsidian联动Deepseek

# Obsidian AI 插件功能对比

| 插件名称                 | 核心定位      | 主要功能                                                             | 适用场景                                | 与本地知识库的关系                                           |
| -------------------- | --------- | ---------------------------------------------------------------- | ----------------------------------- | --------------------------------------------------- |
| **Copilot**          | AI 知识库引擎  | 为笔记建立向量索引，实现语义检索；基于整个笔记库进行问答和知识管理；支持本地模型（Ollama）                 | 搭建个人知识库、对大量笔记进行智能检索、让 AI 基于笔记内容回答问题 | **核心组件**。负责连接 Ollama + BGE + Qwen，实现知识库的检索增强生成（RAG） |
| **Smart Composer**   | AI 写作辅助工具 | 通过 `@` 引用指定笔记/文件夹作为上下文；辅助写作、润色、校对；支持图片和网页链接作为输入                  | 专注当前笔记创作、需要 AI 根据指定材料辅助写作时          | **辅助工具**。可作为 Copilot 的补充，用于创作场景，但不具备知识库索引能力         |
| **AI for Templater** | 自动化模板引擎   | 在 Templater 模板中通过语法调用 AI；自动为新笔记生成摘要、标签或文件名；支持批量处理                | 需要自动化批量处理笔记、新建笔记时自动包含 AI 内容时        | **辅助工具**。用于知识库的自动化维护，不直接参与问答检索                      |
| **Clipper**          | 网页内容抓取工具  | 将网页、社交媒体内容一键保存为 Markdown 文件到仓库；支持多种平台（Facebook、Twitter、Reddit 等） | 需要从网络收集资料到 Obsidian、为知识库提供素材时       | **内容输入源**。负责为知识库补充外部数据，不参与 AI 推理                    |

## Copilot、Smart Composer

1. 进入Obsidian，在插件市场安装Copilot、Smart Composer插件并启用

![[attachment/69f97b14a3986e4bc8cc64a1af19689a_MD5.png]]

2. 打开Copilot插件设置页面，在模型选项卡按照以下内容填写。

![[attachment/3defd4a2a72f720b106db9d7306fb580_MD5.png]]

3. 在对话窗口切换对话模型，就可以使用了。

![[attachment/388b3737537d01ea59a0e44aa2002e51_MD5.png]]

4. 使用同样的方法，安装嵌入模型，按照以下步骤安装，模型名称为“bge\-m3”

![[attachment/86d019b7de1dc74ba3af0edbb1b7d5c9_MD5.png]]

5. Smart Composer同样的操作。
6. 将Copilot的搜索方式更改为语义搜索
[[attachment/2ecbf09829fa3e6f36f442a450de546c_MD5.jpg|Open: Pasted image 20260706191412.png]]
![[attachment/2ecbf09829fa3e6f36f442a450de546c_MD5.jpg]]

## AI for Templater

AI for Templater使用本地本地部署的模型方式：

1. 打开插件设置，AI for Templater选项面板，调整 Endpoint 为 http://127\.0\.0\.1:11435/v1（一定要加上/v1）

2. API Key 填写 no\-key\-needed\(不能留空，中间要加上占位符\)

3. Model Name：deepseek\-r1:14b

![[attachment/a8be1064a5f53cc4a60040efbab52a6e_MD5.png]]

# Obsidian Copilot预设指令与模板

设置位置：

![[attachment/ce7a099dd1d99c95826cd60cccbd3397_MD5.png]]

## 名称：生成问题清单

**用途**：从笔记中提取或生成问题清单，适合学习、研究或会议记录。

**Prompt**：

```Plain Text
<instruction>识别选中文本中的问题或不确定内容，并以 Markdown 编号列表格式输出（1. 问题）。如果没有找到问题，则根据内容生成 2-3 个相关问题。仅返回问题列表。</instruction>
<text>{copilot-selection}</text>
```

## 名称：整理会议记录为结构化笔记

- **用途**：将杂乱的会议记录整理为结构化笔记，适合工作场景。

- **Prompt**：

```Plain Text
<instruction>将选中的会议记录整理为结构化的 Markdown 格式。添加标题（## 会议记录），并为关键部分创建子标题，包括“参会人员”、“议程”和“行动项”。“参会人员”格式化为列表（- 姓名），“议程”格式化为编号列表（1. 主题），“行动项”格式化为任务列表（- [ ] 任务）。保留所有原始内容和含义。仅返回结构化笔记。</instruction>
<text>{copilot-selection}</text>
```

## 名称：提取关键词并生成标签

- **用途**：从笔记中提取关键词并生成 Obsidian 标签，方便分类和 Dataview 查询。

- **Prompt**：

```Plain Text
<instruction>分析选中文本，提取 3-5 个代表主要主题的关键词或短语。以 Markdown 标签列表格式输出（- #标签）。仅返回标签列表。</instruction>
<text>{copilot-selection}</text>
```

## 名称：整理代码块为注释文档

- **用途**：将代码块整理为带注释的文档，添加代码说明和用途。

- **Prompt**：

```Plain Text
<instruction>分析选中的代码块，为其添加 Markdown 格式的注释文档。添加子标题（## 代码说明），并在代码块前添加描述（- **用途**：用途 - **说明**：说明）。将代码块格式化为 Markdown 代码块（`语言\n代码\n`）。保留原始代码内容。仅返回整理后的代码文档。</instruction>
<text>{copilot-selection}</text>
```

## 名称：将文本转为表格

- **用途**：将列表或段落转换为表格的，适合整理数据或对比信息。

- **Prompt**：

```Plain Text
<instruction>将选中文本转换为 Markdown 表格。分析文本内容，识别合适的列（如名称、描述、状态），并以表格格式输出（| 列1 | 列2 | ... |）。保留所有原始内容和含义。仅返回格式化的表格。如果无法转换为表格，则返回“无法转换为表格”。</instruction>
<text>{copilot-selection}</text>
```

## 名称：提取引文并格式化

- **用途**：从文本中提取引文（带引号的句子），并格式化为 Markdown 引用样式。

- **Prompt**：

```Plain Text
<instruction>从选中文本中提取所有引文（带引号的句子，例如“内容”），并以 Markdown 引用格式输出（> 引文）。如果引文有来源信息（例如“——作者”），则保留并附在引文后。仅返回格式化后的引文列表。如果没有找到引文，则返回“未找到引文”。</instruction>
<text>{copilot-selection}</text>
```

## 名称：提取人物信息并整理为表格

- **用途**：从文本中提取人物信息（如姓名、角色、联系方式），并整理为 Markdown 表格。

- **Prompt**：

```Plain Text
<instruction>从选中文本中提取人物信息（例如姓名、角色、联系方式），并以 Markdown 表格格式输出（| 姓名 | 角色 | 联系方式 |）。如果未找到相关信息，则返回“未找到人物信息”。仅返回整理后的表格或提示信息。</instruction>
<text>{copilot-selection}</text>
```

## 名称：将文本转为 FAQ 格式

- **用途**：将文本中的问答内容整理为 FAQ 格式，适合知识整理或文档编写。

- **Prompt**：

```Plain Text
<instruction>分析选中文本，识别问答内容（例如问题和答案），并以 Markdown FAQ 格式输出。使用子标题（### 问题）和正文（答案）格式。如果没有明确的问答内容，则根据文本生成 2-3 个相关问答。仅返回格式化的 FAQ 内容。</instruction>
<text>{copilot-selection}</text>
```

## 名称：提取关键词并链接笔记

- **用途**：从文本中提取关键词并生成 Obsidian 双向链接格式。

- **Prompt**：

```Plain Text
<instruction>从选中文本中提取 3-5 个关键词（名词或短语），并将其转换为 Obsidian 双向链接格式（[[关键词]]）。以 Markdown 列表（- [[关键词]]）输出，仅返回关键词链接列表。</instruction>
<text>{copilot-selection}</text>
```

## 名称：提取网址并附上网址说明

- **用途**：从文本中提取所有网址，并生成带有简短说明的 Markdown 链接列表。

- **Prompt**：

```Plain Text
<instruction>从选中文本中提取所有网址（例如 https://example.com），并生成 Markdown 链接列表（- [描述](网址)）。如果网址前有上下文描述，则使用上下文作为描述；否则使用“链接”作为默认描述。仅返回整理后的链接列表。</instruction>
<text>{copilot-selection}</text>
```

# 常用模板

常用模板，解压后直接拖到Obsidian即可导入

\[常用模板\.rar\]

# 常见问题

## 1.知识库未切片

### 强制重建索引并观察模型是否被加载

1. 在 Obsidian 中按 `Ctrl/Cmd + P`
    
2. 执行 `Copilot: Force Reindex Vault`
    
3. **立即切换到命令行**，执行 `ollama ps`，观察是否有模型出现
    
4. 如果索引开始运行，你应该会看到 **bge-m3** 出现在列表中（因为索引过程需要调用嵌入模型）
5. 检查是否切换搜索方式
[[attachment/2ecbf09829fa3e6f36f442a450de546c_MD5.jpg|Open: Pasted image 20260706191510.png]]
![[attachment/2ecbf09829fa3e6f36f442a450de546c_MD5.jpg]]
# Obsidian与插件安装包

Windows系统：

\[Obsidian\-1\.8\.9\.exe\]

Mac系统：

\[Obsidian\-1\.8\.9\.dmg\]

常用插件：

\[plugins\.rar\]

## 插件安装方法：

### 方法一：从“社区插件市场”安装（可能部分插件需要网络环境导致无法加载社区插件）

1. 关闭安全模式

2. 直接打开社区插件市场即可

![[attachment/1c4196ddb2a171d32b3c4da12c98c233_MD5.png]]

![[attachment/9054f0ed1618ba9146d78282763324b2_MD5.png]]

### 方法二：将插件文件夹复制到\.obsidian文件夹内

**安装说明**：

- **解压文件**：将 \.zip 文件解压，得到插件文件夹。

- **放入插件目录**：

![[attachment/e3e4d6e817b63538c14f849bbc46daa5_MD5.png]]

![[attachment/784d506091431800db70e08a865de5c2_MD5.png]]

    - 找到 Obsidian 的 vault（笔记库）文件夹。

    - 直接将解压得到的plugin文件夹粘贴到 \.obsidian文件夹

    - 最终路径应为：你的 vault 路径/\.obsidian/plugins。

![[attachment/dd9587b71bcbed5e22cd0803a989686b_MD5.png]]

- **启用插件**：

    - 打开 Obsidian，进入“设置” \> “社区插件”。

    - 确保“安全模式”已关闭（如果未关闭，点击“关闭安全模式”）。

    - 在“已安装的插件”列表中找到新添加的插件（例如 Dataview），点击“启用”。

![[attachment/c0052671fcbf32c866c5d32160d1e145_MD5.png]]

- 可能需要重启 Obsidian 以确保插件加载。



