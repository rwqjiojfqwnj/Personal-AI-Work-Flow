# 项目基线模板

> 用途：新项目初始化时填写一次，作为团队默认工程约束。  
> 建议由后端、前端、负责人共同确认。

---

## 1. 项目信息

- 项目名称：
- 项目类型：
- 团队规模：
- 前端技术栈：
- 后端技术栈：
- 是否前后端分离：

---

## 2. AI 工具基线

- 是否使用 Comet：
- 是否使用 OpenSpec：
- 是否使用 Superpowers：
- 是否使用 codegraph：
- 是否使用 Impeccable：

---

## 3. 前端基线

- 组件库：`Element Plus`
- 路由方案：
- 状态管理方案：
- 请求层封装方式：
- 表单规范：
- 表格规范：
- 主题变量位置：
- 是否支持暗色模式：

### 前端样式策略

- AI 可直接编写的范围：
- 必须走 Impeccable 的页面类型：
- 是否启用 Impeccable hook / detector：
- 是否使用额外样式工具（如 UnoCSS）：

### 默认组件库结论

> 新项目默认固定为 `Element Plus`。  
> 只有在项目已有成熟设计系统或明确历史包袱时，才评估是否保留其他组件方案。

---

## 4. 后端基线

- 接口风格：`REST / RPC / 混合`
- 统一返回结构：
- 统一异常处理：
- 分页对象规范：
- 权限模型：
- 幂等策略：
- 日志规范：
- 审计规范：

---

## 5. 数据与缓存基线

- 数据库：
- ORM / Mapper：
- Redis 使用范围：
- 缓存失效策略：
- 大字段查询约束：
- 长事务控制策略：

---

## 6. 环境与配置

- 本地环境：
- 测试环境：
- 生产环境：
- 环境变量管理方式：
- 敏感配置管理方式：
- 三方接口密钥注入方式：

---

## 7. Git 与协作基线

- 分支模型：
- Commit 规范：
- PR 规范：
- Code Review 规则：
- 发布节奏：

---

## 8. 默认非功能约束

- 性能约束：
- 安全约束：
- 可观测性约束：
- 可维护性约束：

---

## 9. 默认验证要求

- 后端最小验证命令：
- 前端最小验证命令：
- 联调要求：
- 验收矩阵是否强制：



